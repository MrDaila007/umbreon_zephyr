# Race / track build — strip UART0 debug overhead (console, logs, bench commands).
# WiFi telemetry on UART1 is unchanged.

CONFIG_CONSOLE=n
CONFIG_UART_CONSOLE=n
CONFIG_LOG=n
CONFIG_PRINTK=n

CONFIG_APP_DEBUG_UART_CMD=n
CONFIG_APP_RUN_PROFILING=n
